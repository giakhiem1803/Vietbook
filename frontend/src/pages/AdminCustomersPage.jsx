import { useEffect, useState } from 'react';
import { customersApi } from '../api/customersApi';
const AdminCustomersPage = () => { const [customers, setCustomers] = useState([]); const [error, setError] = useState(''); useEffect(() => { customersApi.getAll().then(setCustomers).catch(() => setError('Không tải được dữ liệu khách hàng.')); }, []); return <div className="page"><h2 className="section-title">Thông tin khách hàng</h2><p className="section-sub">Dữ liệu riêng tư — chỉ tài khoản quản trị được phép xem.</p>{error ? <div className="alert alert-error">{error}</div> : <div className="card"><table className="table"><thead><tr><th>Khách hàng</th><th>Email</th><th>Số điện thoại</th><th>Địa chỉ nhận hàng</th></tr></thead><tbody>{customers.map((c) => <tr key={c.id}><td>{c.full_name}</td><td>{c.email}</td><td>{c.phone || '—'}</td><td>{c.address || '—'}</td></tr>)}</tbody></table></div>}</div>; }; export default AdminCustomersPage;
