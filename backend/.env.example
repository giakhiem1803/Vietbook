# Local only. Render injects DATABASE_URL from its linked PostgreSQL instance.
DATABASE_URL=sqlite:///./vietbook.db
SECRET_KEY=replace-with-a-long-random-secret
FRONTEND_ORIGINS=http://localhost:5173
# QR MoMo/VietQR is manually confirmed by an Admin; no merchant credentials are required.
